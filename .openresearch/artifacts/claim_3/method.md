# Method, limitations, and deviations

Instruments calls to the official basis evaluator without changing its outputs, then compares against a separately coded rank simulator. Runtime is reported but the verdict rests on exact operation counts. Deviation: the paper HTML itself does not contain the judge record's explicit O(r³) wording.
