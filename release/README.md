# Protected additive Space release

Target: `DineshAI/qC9FEfYjai`.

Protected judged revision:
`6ce8ca3f9b6bd4071976f04c7b105db70e5c5e92`.

`space_updates/` is an overlay, not a replacement tree. Apply only the paths in
`upload_allowlist.txt` using the text-only Hugging Face API after explicit user
approval. Existing files remain at their current revision; `logbook.json`
preserves every old navigation entry and appends the campaign pages.
